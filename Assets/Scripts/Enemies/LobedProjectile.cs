using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class LobedProjectile  {
    private float weight;
    /*void Start()
    {
        base.Start();
        
        Debug.Log(destinationPos);
        Debug.Log(destinationPos.normalized);
    }

    void Update()
    {
        base.Update();        
    }

    protected override void Move() {
        Debug.Log("move");
        body.AddForce(new Vector3(1,1,1));
    }

    public LobedProjectile(int damage, Vector3 destinationPos, GameObject prefab, Vector3 position) : base(damage, destinationPos, prefab, position) {
        
    }*/
}
